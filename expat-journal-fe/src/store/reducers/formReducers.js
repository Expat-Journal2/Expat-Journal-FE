export const initialState = {
    fullname: "",
    username: "",
    password: ""
}

export const formReducer = (state = initialState, action) => {
    switch (action.type){
        default:
            return state;
    }
}